{% extends 'base.html.twig' %}


{% block body %}

<div class="row">
    {% for product in products %}
        <div class="col-sm-6 col-md-4">
            <div class="thumbnail">
                <img src="{{ '/uploads/images/products/' ~ product.image }}" alt="..." class="img-responsive">
                <div class="caption">
                    <h3>{{ product.name }}</h3>
                    <h2>{{ product.price }} €</h2>
                    <p>{{ product.description }}</p>

                    <p><a href="#" class="btn btn-primary" role="button">Voir plus</a>
                </div>
            </div>
        </div>
    {% endfor %}
</div>

{% endblock %}