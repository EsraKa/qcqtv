

{% block title %}AppBundle:App:result{% endblock %}


{% block body %}
    <div class="container">
        <div class="row">
            {% for product in products %}
                <div class="col-sm-3 col-md-3">
                    <div class="thumbnail">
                        <img src="{{ asset('/uploads/images/products/' ~ product.image) }}" alt="..." class="img-responsive" style="max-height: 200px">
                        <div class="caption">
                            <h3>{{ product.name }}</h3>
                            <h2>{{ product.price }} €</h2>

                            <p><a href="#" class="btn btn-primary" role="button">Voir plus</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>


{% endblock %}
